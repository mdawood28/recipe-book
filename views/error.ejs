<%- include('partials/head') %>
    <%- include('partials/navbar') %>

        <main class="flex-grow flex items-center justify-center py-20 px-4">
            <div class="text-center max-w-lg">
                <div class="text-8xl mb-8">🍽️</div>
                <h1 class="font-serif text-4xl font-bold text-charcoal mb-4">Oops!</h1>
                <p class="text-xl text-gray-600 mb-8">
                    <%= typeof message !=='undefined' ? message : 'Something went wrong.' %>
                </p>
                <a href="/"
                    class="inline-block bg-accent hover:bg-accent-dark text-white px-8 py-3 rounded-full font-semibold transition-all shadow-md hover:shadow-lg">Return
                    to Kitchen</a>
            </div>
        </main>

        <%- include('partials/footer') %>